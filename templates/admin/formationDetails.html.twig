{% extends 'admin/admin-template.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block content %}
    <div class="container">

        <div class="row">
            <div class="col">
                <h2>{{ title }}</h2>
                <div class="formation-details">
                    <span>Date de début : {{ formation.dateDebut|format_datetime('full', 'none', locale='fr') }}</span>
                    <br />
                    <span>Date de fin : {{ formation.dateFin|format_datetime('full', 'none', locale='fr') }}</span>
                </div>
            </div>
            <div class="col">
                <h3>Informations</h3>
                <div class="formation-details">
                    <span>Nombre d'intervenant : {{ qteIntervenant }}</span><br />
                    <span>Heures de cours assignées : {{ qteHeureAttribue // 60 }}h {{ qteHeureAttribue % 60 }}m</span>
                </div>
            </div>




        </div>
    </div>
{% endblock %}